<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
        </shape>
    </item>

    <!-- Bottom Border with rounded corners -->
    <item android:bottom="4dp" android:left="0dp" android:right="0dp">
        <shape android:shape="rectangle">
            <solid android:color="#1E88E5"/> <!-- Change color here if desired -->
            <size android:height="2dp"/> <!-- Border thickness -->
            <corners android:bottomLeftRadius="8dp" android:bottomRightRadius="8dp"/> <!-- Bottom corner radius -->
        </shape>
    </item>
</layer-list>
